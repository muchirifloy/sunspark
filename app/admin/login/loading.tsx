/**
 * The login screen has no sidebar, so the admin shell skeleton from the parent
 * segment would flash a layout the page never renders. Nothing is the better
 * placeholder here - the form resolves immediately.
 */
export default function AdminLoginLoading() {
  return null;
}
